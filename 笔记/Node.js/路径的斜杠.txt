路径中的正斜杠和反斜杠

1. 系统目录

在windows中目录为 \ 表示目录，  unix系统中是 / 表示目录

2. web路径

由于web遵循Unix命名，所有在网址（URL）中，/ 表示目录， 
在html url() 属性中，指定的是网络路径，必须用斜杠 / 

3. 代表路径不同

出现在普通字符串中的路径，如果代表的是Windows文件路径，则使用斜杠/ 和
反斜杠\是一样的，如果是网络文件路径，则必须使用/， 
../ 代表上一级目录
./代表当前目录